<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Repository of Cool Names</title>
    <!-- Bootstrap CSS link -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css">
</head>
<body class="container">
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <a class="navbar-brand" href="#">Repository of Cool Names</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="/form">Add Name</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/test">Test</a>
                </li>
            </ul>
        </div>
    </nav>

    <!-- Jumbotron -->
    <div class="jumbotron">
        <h1 class="display-4">Welcome to the Repository of Names!</h1>
        <p class="lead">This is a collection of names stored in a database. Below you will find a table with all the stored names:</p>
    </div>

    <!-- Table -->
    <div class="row">
        <div class="col">
            <table class="table table-striped">
              <thead>
                <tr>
                  <th>First Name</th>
                  <th>Last Name</th>
                  <th>Full Name</th>
                </tr>
              </thead>
              <tbody>
                <% names.forEach(name => { %>
                  <tr>
                    <td><%= name.firstname %></td>
                    <td><%= name.lastname %></td>
                    <td><%= name.fullname %></td>
                  </tr>
                <% }); %>
              </tbody>
            </table>
        </div>
    </div>
      
    <div class="row">
        <div class="col">
          <% if (currentPage > 1) { %>
            <a href="?page=<%= currentPage - 1 %>" class="btn btn-primary">&lt; Previous</a>
          <% } %>
          <% if (currentPage < totalPages) { %>
            <a href="?page=<%= currentPage + 1 %>" class="btn btn-primary ml-2">Next &gt;</a>
          <% } %>
        </div>
    </div>

    <!-- Links -->
    <br>
    <div class="row">
        <div class="col">
            <p>If you want to add more names, please click <a href="/form">here</a> to go to the input form.</p>
            <p>If you want to see test, please click <a href="/test">here</a> to go to the test page.</p>
        </div>
    </div>

    <!-- Bootstrap JS scripts -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/js/bootstrap.min.js"></script>
</body>
</html>


